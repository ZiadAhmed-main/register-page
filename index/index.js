let img = document.querySelector('.imgsrc');
let container = document.querySelector('.container');
function phones(newsource) {
    img.src = newsource;
}

function colors(color) {
    container.style.background = color;
}